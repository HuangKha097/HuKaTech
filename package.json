{
  "name": "doancnpm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "start": "npm-run-all --parallel server client",
  "server": "cd backend && npm start",
  "client": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
